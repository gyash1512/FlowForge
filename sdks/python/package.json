{
  "name": "@flowforge/python-sdk",
  "version": "0.1.0",
  "private": true,
  "description": "Python SDK for FlowForge (workspace shim)",
  "scripts": {
    "test": "python3 -m pytest || echo 'pytest not installed, skipping'",
    "test:ci": "python3 -c 'import pytest' 2>/dev/null || exit 0; python3 -m pytest --tb=short",
    "lint": "ruff check . || echo 'ruff not installed, skipping'",
    "lint:fix": "ruff check --fix . || echo 'ruff not installed, skipping'",
    "typecheck": "mypy flowforge_sdk || echo 'mypy not installed, skipping'",
    "clean": "rm -rf dist .mypy_cache .pytest_cache .ruff_cache *.egg-info"
  }
}
